---
import TitleSection from '../../components/TitleSection.astro'

import linked from '../../assets/linkedin.svg'
const {gitIcon} = Astro.props

---
<div
  class="border-1 border-white/10 rounded p-5 shadow-black shadow-lg">
  <TitleSection text="📬 Contact"/>
  <div
    class="py-4 inline-flex items-center gap-5 text-[10px] sm:text-sm">
  <a 
    class="inline-flex items-center gap-2 text-white bg-black px-4 py-2 rounded-4xl"
    href="https://github.com/MEZLYSO">
     <img
        class="size-5 m-1 bg-white p-1 rounded-4xl"
        width={gitIcon.width}
        height={gitIcon.height}
        src={gitIcon.src} 
        alt={gitIcon.name}> 
      Github
    </a>
  <a 
    class="inline-flex items-center gap-2 text-white bg-black px-4 py-2 rounded-4xl"
    href="https://github.com/MEZLYSO">
     <img
        class="size-5"
        width={gitIcon.width}
        height={gitIcon.height}
        src={linked.src} 
        alt={gitIcon.name}> 
      LinkedIn
    </a>
  </div>
</div>
